#include "gtest/gtest.h"
#include "session.h"

// TODO: Finish MockSocket
TEST(SessionTest, SessionShouldOutput) {
	// boost::asio::io_service io_service;
	// MockSocket socket(io_service);
	// Session s(socket);
	// io_service.run();
	// socket.write("GET / HTTP/1.0")
	// ASSERT_EQUALS(socket.get_data(), "some response");

	ASSERT_TRUE(true);
}